The project done is SQL data cleaning one and it contains a dataset with Nashville Housing information.I used Microsoft SQL Server to execute queries that helped with converting date formats or standardizing dates, populating missing data, splitting data into new columns, removing duplicates, and deleting unnecessary columns. 
